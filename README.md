# space-invaders-3D-opengl-2

## Build the Project
Navigate to the root directory of the project and run:

```mvn clean compile
```
## Run the Application
Navigate to the root directory of the project and run:

```mvn exec:java
```